name: noctoolf_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  go_router: ^16.1.0
  path: ^1.9.1
  process: ^5.0.5
  provider: ^6.1.5
  uuid: ^4.5.1
  # Note: All platform plugins removed to avoid symlink creation on this FS.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
